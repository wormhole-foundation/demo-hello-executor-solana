# ── Keys ──────────────────────────────────────────────────────────────────────
# Solana keypair: JSON array of bytes OR a path to a keypair file
# PRIVATE_KEY_SOLANA=[1,2,3,...]
SOLANA_KEYPAIR_PATH=~/.config/solana/id.json

# Sepolia private key (used by setupPeers.ts to register the Solana emitter PDA)
PRIVATE_KEY_SEPOLIA=0x...

# ── RPC endpoints (optional — defaults are public nodes) ─────────────────────
# SOLANA_DEVNET_RPC=https://api.devnet.solana.com
# SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# ── Contract / program addresses ─────────────────────────────────────────────
# These default to the latest deployed values in config.ts.
# Override here after a fresh deployment.
#
# TODO(redeploy): After deploying a new Solana program binary, update both of
# these to match the new deployment, then re-run initialize.ts and setupPeers.ts.

# HELLO_EXECUTOR_SOLANA=7eiTqf1b1dNwpzn27qEr4eGSWnuon2fJTbnTuWcFifZG
# HELLO_WORMHOLE_SEPOLIA=0x15cEeB2C089D19E754463e1697d69Ad11A6e8841
